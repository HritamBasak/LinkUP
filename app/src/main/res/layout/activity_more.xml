<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/scrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light"
    android:padding="16dp"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <!-- App Icon -->
        <ImageView
            android:id="@+id/imageView3"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/team"
            android:elevation="4dp"
            android:padding="20dp"
            android:src="@drawable/info" />

        <!-- App Name -->
        <TextView
            android:id="@+id/textView19"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            android:fontFamily="@font/afacad_bold"
            android:text="LinkUp"
            android:textColor="@color/black"
            android:textSize="28sp"
            android:textStyle="bold" />

        <!-- Version -->
        <TextView
            android:id="@+id/textView20"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Version: 1.0"
            android:textColor="#6D6767"
            android:textSize="14sp"
            android:layout_marginBottom="20dp" />

        <!-- Info Options Card -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            app:cardElevation="8dp"
            app:cardCornerRadius="16dp"
            app:cardBackgroundColor="@android:color/white">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- Menu Items -->
                <TextView
                    android:id="@+id/aboutUs"
                    style="@style/MoreItemStyle"
                    android:text="About Us"
                    android:drawableLeft="@drawable/resize_info" />

                <View style="@style/DividerLine"/>

                <TextView
                    android:id="@+id/terms"
                    style="@style/MoreItemStyle"
                    android:text="Terms &amp; Conditions"
                    android:drawableLeft="@drawable/resize_terms" />

                <View style="@style/DividerLine"/>

                <TextView
                    android:id="@+id/privacy"
                    style="@style/MoreItemStyle"
                    android:text="Privacy Policy"
                    android:drawableLeft="@drawable/resize_privacy" />

                <View style="@style/DividerLine"/>

                <TextView
                    android:id="@+id/share"
                    style="@style/MoreItemStyle"
                    android:text="Share App"
                    android:drawableLeft="@drawable/resize_share" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>

    </LinearLayout>
</ScrollView>
